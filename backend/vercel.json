{
    "version": 2,
    "builds": [
        {
        "src": "backend/main.py",
        "use": "@vercel/node"
        }
    ],
    "routes": [
        {
        "src": "/api/(.*)",
        "dest": "/backend/main.py"
        }
    ]
}